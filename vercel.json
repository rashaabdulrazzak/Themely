{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
   "builds": [
    { "src": "src/index.tsx", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/src/index.tsx" }
  ]
}
